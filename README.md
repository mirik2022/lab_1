## 📌 Описание проекта
Данный проект выполнен в рамках лабораторной работы по дисциплине 
"Объектно-ориентированное программирование". Цель работы — изучение 
и практическое применение концепций ООП: наследования, полиморфизма 
(виртуальных методов) и инкапсуляции.

## 🏗️ Архитектура
В программе реализована иерархия классов для представления 
геометрических фигур:
- `Shape` — абстрактный базовый класс
- `Line` — отрезок
- `MyRectangle` — прямоугольник
- `Square` — квадрат (наследуется от `MyRectangle`)
- `Ellipse` — эллипс
- `Circle` — круг (наследуется от `Ellipse`)
- `Triangle` — треугольник

Все фигуры хранятся в отдельном классе-коллекции `ShapeList`, 
что обеспечивает инкапсуляцию логики работы со списком фигур.
